const branch = require('./branch');
const user = require('./user');
const mealplan = require('./meal-plan');
const mealmaster = require('./meal-master');
const mealtime = require('./meal-time');
const reservation = require('./reservation');
const mealExtra = require('./meal-extra');
const auth = require('./auth');

module.exports = {
  branch,
  user,
  mealplan,
  mealmaster,
  mealtime,
  reservation,
  mealExtra,
  auth
}